export interface answerFace {
  questionId: string
  answer: number
  title: string
  choices: string
  tag: string
}

export interface questionFace {
  answer: number
  title: string
  choices: string
  tag: string
}
